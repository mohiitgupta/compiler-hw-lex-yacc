j=0;
if (j==0) /* testing IF*/  while (j<10) /* testing a loop */
     { /* testing compound */
        Println(j, j+1, j*2+(2-1));
         j=j+1;
     }
else j=1;


if ((0<j) && (j==10)) { /* testing && and else if */  
   Println(j, j-1, j*j+(j-j)-j);
   j=j-1;
   j=0;
   i=5;
	if (j==0) /* nesting IF */
		while (j<i+1) 
         	{ /* Nesting while */
		    if j<3 Println(j); else Println(0-j);
			j=j+1;
		}
}
Print( j);


scp -r HW5test.in input.in y.output y.vcg Makefile hw5.l lex.yy.c y.tab.c hash.h hw5.y struct_funcs.h y.tab.h gupta440@xinu01.cs.purdue.edu:~/hw5/

